import React from "react";
import '../ComponentsStyle.css';

export const Home = props => {
    return (
            <div className="home">
                Budget Manager
            </div>
    );
};

Home.propTypes = {};